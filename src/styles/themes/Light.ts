import { createTheme } from '@stitches/react'

export const GlobalStyles = createTheme({
  colors: {
    primary: '#3eb489',
    secundary: 'rgba(30, 32, 36, 0.7)',
    white: '#fff',
    text: '#fff',
    black: '#000',
    dark: '#2c2c2c',
    danger: '#ff7262',
    grey: '#dfdfdf',
    'bg-icon': 'rgba(255, 255, 255, 0.2)'
  }
})

export default GlobalStyles
